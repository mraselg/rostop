# Automated Git Commit & Push Rule

## Directive
After completing every feature, redesign, bug fix, or significant update requested by the user:
1. Stage all changed files.
2. Create a concise, meaningful commit message summarizing what was accomplished.
3. Automatically push to the configured remote Git repository (e.g. `origin main`).
4. Inform the user of the commit hash and push status.
